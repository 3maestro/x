<?xml version="1.0" encoding="UTF-8"?>
<!-- mybatis맵퍼  -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- namespace속성은 인터페이스의 위치를 적는다. -->
  
  
	<!-- 파라미터 타입은 내가 받을 타입을 설정한다 -->
	<!-- 리절트타입은 내가 돌려줄타입을 (리턴타입)설정한다 -->
<mapper namespace="com.cafe24.radev.mapper.CarMapper">
	<!-- id : mapper 관련 메서드명 resultType : vo주소.vo명 -->
	
	<!-- 차량 수정 조회 쿼리 -->
	<select id="getCarUpdate" parameterType="String" resultType="com.cafe24.radev.vo.VoCarRegister">
			SELECT 
			customer_car_info_code AS customerCarInfoCode,
			bs_code AS bsCode,
			customer_code AS customerCode,
			customer_car_number AS customerCarNumber,
			vendor_code AS vendorCode,
			rc_code AS rcCode,
			car_production_year AS carProductionYear,
			car_registration_date AS carRegistrationDate,
			last_distance_driven AS lastDistanceDriven,
			last_visit_date AS lastVisitDate,
			customer_car_info_writer AS customerCarInfoWriter,
			customer_car_info_date AS customerCarInfoDate
			
		FROM
			customer_car_info_table
		WHERE customer_car_info_code = #{cuscar};
	
	</select>
		
	
		<!-- 차량 등록 쿼리 -->
	<insert id="getCarInsert" parameterType="com.cafe24.radev.vo.VoCarRegister" >
		INSERT INTO
			customer_car_info_table
				(customer_car_info_code,
				customer_code,
				customer_car_number,
				vendor_code,
				
				last_distance_driven)
		VALUES(
				#{customerCarInfoCode},
				#{customerCode},
				#{customerCarNumber},
				#{vendorCode},
				#{lastDistanceDriven})
	</insert>
	<!-- 값을 [리턴받아] 사용할때 리절트 타입을 사용한다 -->
	<!-- 차량 리스트 조회 쿼리 -->
	<select id="getCarList" resultType="com.cafe24.radev.vo.VoCarRegister">
		SELECT 
			customer_car_info_code AS customerCarInfoCode,
			bs_code AS bsCode,
			customer_code AS customerCode,
			customer_car_number AS customerCarNumber,
			vendor_code AS vendorCode,
			rc_code AS rcCode,
			car_production_year AS carProductionYear,
			car_registration_date AS carRegistrationDate,
			last_distance_driven AS lastDistanceDriven,
			last_visit_date AS lastVisitDate,
			customer_car_info_writer AS customerCarInfoWriter,
			customer_car_info_date AS customerCarInfoDate
			
		FROM
			customer_car_info_table
	</select>

	
</mapper>