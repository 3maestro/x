<?xml version="1.0" encoding="UTF-8"?>
<!-- mybatis맵퍼  -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- namespace속성은 인터페이스의 위치를 적는다. -->
<mapper namespace="com.cafe24.radev.mapper.PartMapper">
	<!-- id : mapper 관련 메서드명 resultType : vo주소.vo명 -->
	<select id="getPartList" resultType="com.cafe24.radev.vo.Part">
		SELECT 
			part_number as partNumber,
			ck_big_code as bigCate,
			ck_mid_code as midCate,
			part_use_name as partName,
			part_use_many as partMany,
			part_use_price as partPrice,
			pu_writer as partWrite,
			pu_date as partUpdateDate
		FROM
			part_use_tb
	</select>
	<select id="partSelectForOrder" resultType="com.cafe24.radev.vo.Part">
		SELECT 
			part_number as partNumber,
			ck_big_code as bigCate,
			ck_mid_code as midCate,
			part_use_name as partName,
			part_use_many as partMany,
			part_use_price as partPrice,
			pu_writer as partWrite,
			pu_date as partUpdateDate
		FROM
			part_use_tb
		WHERE
			part_number=#{partNumber}
	</select>
	
	
</mapper>